# KeyboardGesture Flow

`KeyboardGesture` --> `KeyboardGestureService` --> `KeyboardGestureRepository`

```
KeyboardGesture
+ shorthand
+ value
+ type
+ has_no_duplicates()

KeyboardGestureService
+ get_all_keyboard_gestures()
+ create(...)
+ update(...)
+ delete(...)
```
